<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>yorijori</title>
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="stylesheet" href="/css/funding/fundingPage.css">
</head>

<body>
  <div id="container">
  <div class="main">
    <!-- 참여한 펀딩 또 참여하려고할때 플레시 메시지보여줌 -->
    <div class="flashes"> 
      <% if(flashMessages) {%>
        <% if(flashMessages.info) {%>
          <div class="flash_info">
            <%= flashMessages.info %>
          </div>
      <% }%>
        <%}%>
    </div>
    <div class="search-container">
      <div class="search">물품 검색: </div>
      <form id="search-form" class="search-form">
        <input type="text" name="query" id="search-input" placeholder="펀딩 검색" class="search-input">
        <button type="submit" class="search-btn">
          <i class="fa-solid fa-magnifying-glass"></i>
        </button>
      </form>
    </div>
      <!-- DB에 목록이 있을 때 -->
      <div>
      <% if (Object.keys(products).length > 0) { %> 
      <div class="fundingRegion">
        지금 <%= products[0].district %>에서 열린 공동 펀딩은..
      </div>
      <div class="funding-container">
        <% products.forEach(product=> { %>
          <div class="funding-card">
            <!-- 목록마다 링크 다르게해야함. 그룹아이디로 구별하기. -->
            <button class="funding-list-btn" data-id="<%= product.fundingGroupId %>">
              <div class="img-box">
                <img src="<%= product.imageUrl %>" alt="productImage" class="img">
              </div>
              <div class="information-box">
                <p class="information">품명: <%= product.productName %> <%=product.quantity%>개</p>
                <p class="information">펀딩 대표자: <%= product.name %></p>
                <p class="information">단위 당 가격: <%= product.unit %>개 / <%= product.unitPrice %>원</p>
              </div>
            </button>
      </div>
      <% }) %>
      <!-- DB에 목록이 없을 때 -->
      <%} else{%>
        <div class="noItems">
          펀딩이 없습니다.
        </div>
        <%}%>
  </div>
  <div class="OpenFuding-box">
    <div class="in">
      내가 필요한 물품에 대한 펀딩이 없다면?
    </div>
    <!-- 펀딩생성하는 페이지로 이동하도록 수정필요 -->
      <a href="/createfundingPage/create_funding_search" class="open">  
      <div class="funding-btn">펀딩 열기
      </div>
    </a>
  </div>
</div>
  </div>
</div>
</body>
<script>
  // 검색 시 fetch + 토큰
  document.getElementById('search-form').addEventListener('submit', function (e) {
    e.preventDefault();
    const token = localStorage.getItem('token');
    const query = document.getElementById('search-input').value;
    const url = `/joinfundingPage/fundingSearch?query=${encodeURIComponent(query)}`;

    fetch(url, {
      method: 'GET',
      headers: token ? { 'Authorization': 'Bearer ' + token } : {}
    })
    .then(res => res.text())
    .then(html => {
      document.open();
      document.write(html);
      document.close();
    })

    .catch(err => alert('검색 실패: ' + err.message));
  });

  // 펀딩 상세 페이지 fetch + 토큰
  document.querySelectorAll('.funding-list-btn').forEach(btn => {
    btn.addEventListener('click', () => {
      const token = localStorage.getItem('token');
      const groupId = btn.dataset.id;
      const url = `/joinfundingPage/joinFunding/${groupId}`;

      fetch(url, {
        method: 'GET',
        headers: {
          'Authorization': 'Bearer ' + token
        }
      })
      .then(res => {
        if (!res.ok) throw new Error('접근 실패');
        return res.text();
      })
      .then(html => {
        document.open();
        document.write(html);
        document.close();
      })

      .catch(err => alert('접근 실패: ' + err.message));
    });
  });
</script>


</html>